import { RequestAppointment } from './request-appointment';

describe('RequestAppointment', () => {
  it('should create an instance', () => {
    expect(new RequestAppointment()).toBeTruthy();
  });
});
